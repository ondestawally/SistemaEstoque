version: '3.8'

services:
  api:
    volumes:
      - ./src:/app/src:ro
      - ./alembic:/app/alembic:ro
      - ./requirements.txt:/app/requirements.txt:ro
    environment:
      - PYTHONUNBUFFERED=1
